# FrozenPlayer (Open Music Player)

Simple Electron-based desktop music player UI (dark theme) — initial scaffold.

Commands

- Install deps: npm ci
- Run in development: npm start
- Build Windows installer: npm run build

Notes

- This scaffold uses devDependencies for electron and electron-builder.
- Add app icons and signing certificates if you plan to publish installers.
test
